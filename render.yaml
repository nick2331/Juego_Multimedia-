services:
  # ── Frontend Static Site (100% gratis, sin tarjeta) ───────────
  - type: web
    name: phobia-horror-client
    runtime: static
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
